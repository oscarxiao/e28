let app = new Vue({
  el: "#app",
  data: {
    item: [{ quantity: null, name: null }],
    itemNum: null,
    itemName: null

    // item: [{ quantity: null, name: null }],
    // numEach: null,
  }
  // computed: {
  //   total() {
  //     let totalNum = this.itemLists.length;
  //     return totalNum;
  //   }
  // method:{
  //   addItem(){
  //     this.item.push({quantity: this.numEach})
  //     this.item.push({name: this.itemName})

  //   }
});
